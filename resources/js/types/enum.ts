export enum ETransactionStatus {
    PENDING = "pending",
    PAID = "paid",
    PROCESS = "process",
    SHIPPING = "shipping",
    FAILED = "failed",
    COMPLETED = "completed",
}

export enum EProductStatus {
    DRAFT = "draft",
    ACTIVE = "active",
    ARCHIVED = "archived",
}
